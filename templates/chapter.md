---
chapter: "{{N}}"
pov: "{{POV CHARACTER}}"
tense: "{{past}}"
time: "{{in-world timestamp}}"
scene_goal: "{{What must this scene achieve? One sentence.}}"
tension_target: "{{T target 1–5}}"
clarity_target: "{{C target 1–5}}"
form_opening: "{{type of opening, e.g. action / reflection / dialogue / status block}}"
form_ending: "{{type of close, e.g. cliffhanger / calm beat / punchline / open question}}"
form_technique: "{{dominant technique of the chapter — for the form-variance check}}"
gating_checked: false
---

> **Note:** This frontmatter block is working material for the pipeline (POV, tense, T/C targets, gating check). Remove or comment it out before publication. The actual chapter text begins below it.
>
> `gating_checked: true` is set by the conductor only once the pre-flight gate has confirmed that no 🔴-open decision from `canon/canon-decision-register.md` blocks this chapter.
>
> The `form_*` tags feed the **form-variance check**: if three chapters in a row match in opening/ending/technique, the Editor flags monotony.

---

{{The narrative text begins here. One POV character, established tense. The scene opening anchors place/time/POV without a label dump.}}
